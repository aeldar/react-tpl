{
  "name": "react-tpl",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "16.0.0",
    "react-dom": "16.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "flow": "flow",
    "coveralls": "[ -f ./coverage/lcov.info ] && cat ./coverage/lcov.info | coveralls"
  },
  "devDependencies": {
    "react-scripts": "1.0.14",
    "coveralls": "3.0.0",
    "flow-bin": "0.56.0"
  }
}
